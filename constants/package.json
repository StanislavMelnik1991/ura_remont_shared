{
  "name": "shared-constants",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lint-staged": "*",
    "typescript": "*",
    "eslint": "*"
  },
  "lint-staged": {
    "*.{ts,tsx}": [
      "eslint --ext ts --ext tsx --fix"
    ]
  }
}
