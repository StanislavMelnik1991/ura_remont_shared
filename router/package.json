{
  "name": "router",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "schemas": "workspace:*"
  },
  "devDependencies": {
    "eslint": "*",
    "lint-staged": "*",
    "typescript": "*"
  },
  "lint-staged": {
    "*.{ts,tsx}": [
      "eslint --ext ts --ext tsx --fix"
    ]
  }
}
