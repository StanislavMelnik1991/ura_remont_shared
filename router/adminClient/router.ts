import { auth } from './auth';
import { brand } from './brand';
import { common } from './common';
import { type } from './type';

export const adminClientRouter = {
  auth,
  common,
  brand,
  type,
};
